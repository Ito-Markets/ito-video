# Copy to .env.hermes or .env and fill in keys.
# These keys are used by the gen/ and fx/ scripts, not by the core assembly.

OPENAI_API_KEY=
GEMINI_API_KEY=
ELEVENLABS_API_KEY=
FAL_KEY=

# Optional: VideoDB if used for indexing/ingest
VIDEO_DB_API_KEY=
